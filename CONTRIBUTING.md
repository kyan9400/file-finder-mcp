# Contributing

Please keep searches bounded, deterministic, read-only, and portable across Windows and Linux.

Add tests for ranking, traversal, exclusion, or output-schema changes. Run `ruff check .`, `ruff format --check .`, `pytest`, and `python -m build` before opening a pull request.

